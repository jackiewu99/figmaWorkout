type Back @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  demo: AWSURL
  steps: String
  area: String
}

type Workout @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  demo: AWSURL
  steps: String
  area: String
}
 